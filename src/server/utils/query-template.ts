/*
import { Query } from '../';
const all = () => Query('');
const one = () => Query('', []);
const insert = () => Query('', []);
const update = () => Query('', []);
const destroy = () => Query('', []);
const find = () => Query('', []);
export default {
    all,
    one,
    insert,
    update,
    destroy,
    find
}
*/